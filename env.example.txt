# Rename this file to .env and fill in your values.
# NEVER commit .env to git — it's in .gitignore.

# Database (PostgreSQL via Neon or local)
DATABASE_URL="postgresql://user:password@host:5432/safedose?sslmode=require"

# JWT
JWT_SECRET="change-me-to-a-long-random-secret-min-32-chars"
JWT_EXPIRES_IN="7d"

# Fastify API
API_PORT=4000
API_HOST="0.0.0.0"
NODE_ENV="development"

# Redis (optional — for rate limiting / queues)
REDIS_URL="redis://localhost:6379"

# Expo Push Notifications (Expo Go / EAS)
EXPO_ACCESS_TOKEN="your-expo-access-token"

# CORS — allowed origins for the API
CORS_ORIGINS="http://localhost:8081,exp://localhost:8081"

# Encryption key for medication sync payloads (AES-256, 64 hex chars)
ENCRYPTION_KEY="change-me-to-a-64-char-hex-string-for-aes256-encryption-xxxxx"
